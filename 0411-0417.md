4卡24进程，torch框架
- calql要跑10天
- iql要跑3-4天
CalQL尽量使用Jax框架，直接使用论文开源的代码库
IQL也使用官方jax框架
或者使用CORL-JAX，但是要将offline改成o2o，工作量稍微会大一点
这个周末应该能做完

超参数
- 目前来看200k CrescLN问题不大->calql large 
- iql->u-d
- online阶段初始有小drop，但是很快就能恢复，之前eval_steps大的时候没发现

hopper-medium 40-50 

state->action 
(s, a)
discriminator->W(s,a)
(s, a) -> pos 
(s, a_random)->neg
1 - W
-maxQ + (1 - W)BC loss