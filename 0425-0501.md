mujoco上的问题还是比较大
我们当前的超参数尽量与Torch-CORL中的实现对齐，IQL中本来就有自己的超参数设定
```bash
bash run.bash umaze,umaze_diverse,medium_play,medium_diverse,large_play,large_diverse 0,1 10 5 8 calql_noln "--gradual_layernorm=False --enable_distillation=True --actor_distill=True --critic_distill=False"
```
现在最主要的问题是，A100_2上的分数不错，但是A100_8上calql的分数不太理想，不知道是咋回事，可能是实现有一些区别，先等等看吧

IQL->A100_8, CalQL->A100_2
A100_8上在IQL-Antmaze完成整理之后，开始在mujoco上的调参
A100_2上在CalQL-Antmaze完成整理之后，开始在mujoco上的调参，如果当前这一组的运行结果不错(4.26 ver)，那么可以 


当前的A100_8 IQL中我们做了这个修改：
```
  问题总结

  1. AntMaze: 当前项目处理正确 (reward -= 1.0)
  2. MuJoCo: 当前项目对所有环境都做了归一化，但:
    - ActorTSM-IQL 和 cleanCORL halfcheetah/walker2d 配置中 normalize_reward: false
    - 如果启用归一化，online phase 需要同步处理（当前项目缺失此处理）
  3. Online phase 一致性: ActorTSM-IQL 正确实现了 modify_reward_online()，当前项目 MuJoCo 缺失此逻辑
```