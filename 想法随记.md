# Existing Problems
- 如果使用Incremental的方法，也就是在当前的网络结构上添加新的成分，那么这个==新的网络应该如何设计==? 来保证输出不变
- 如果就采用部分冻结的方法，应该冻结的部分是哪一些部分？应该==如何选择这些要被冻结的神经元==？Random Selection吗
- 冻结之后进行训练时，是否需要再加上==Regenerative Norm等技术保证非冻结部分不能有太大的Norm Growth?==
- ==LayerNorm==是否是全程都加上，还是仅在online阶段加上？
- Offline/Online之间的这个Actor和Critic的Gap？
- Data Buffer的转换，online阶段我们是直接新建一个==Online buffer==，还是往原来的==offline buffer中加入新数据==，==还是定比例采样?==
- 注意当前cal-ql部分在online和offline阶段，有==两个buffer==, explicitly控制sampling ratio，而iql在offline和online阶段采用==同一个Buffer,online阶段直接将数据加进去==
- ==How to define plasticity?== 我认为是在一个比较短的时间间隔之内，例如100K步，看一下分数的上升情况。
# 想法随记
## 1. Teacher-Student Method
是否能将Offline Pre-trained Model作为我们的Teacher，通过蒸馏，将Teacher学习到的知识传递给一个Newly-Initialized的网络，这个网络中可以有一部分东西是用来学习的，一部分是暂时冻结住保证网络可塑性的
Teacher-Student Model
如何在保留offline阶段数据的同时，通过一个新的，可塑性强的网络来保留知识并进行新的学习
本质上：能不能用更少的参数保存前面学习到的知识，再用更多数据来进行进一步学习？
==网路对象?Actor还是Critic?==
可以区分offline阶段使用的算法和online阶段使用的算法，例如利用IQL在offline阶段的稳定性
++tricks, layernorm, Entropy Tuning, ...

尽量使用相同的offline和online算法
可以选择做减法(类似于Stay Hungry)，可以选择做加法，加一个残差的head作为新的Plasticity Head
==IQL/CalQL/SAC Tuning==
注意Plasticity的衡量标准
Actor和Critic都试一试
## 2. LoRA for RL
- 相关的可以参考Residual RL, LoRA和Plasticity Injection的思路，在Online阶段引入新的Trainable Head
- ![[Pasted image 20260204121821.png]]
- 可以让alpha递增，保证online初期的安全性
- 或者说我们是不是可以直接在原来的offline网络后加一个映射，和我们原来的CrescLayerNorm的思路类似，缓慢加上一个新的Trainable部分
- LoRA fine-tune可以考虑采用不同的参数量？
- LoRA本身是一个基于残差的旁路
- ==这里还有两种可能可以实现的方法，Residual还是Mapping==
- LoRA都是只针对中间层的，保持输出层不动
- 也许Backbone可以使用很小的学习率，LoRA部分可以使用正常的学习率
# 3. Residual RL
- 学习的是Residual Values，Residual Values使用新训练的网路来实现
- 是否会需要online Pre-Training? 例如先通过offline的Actor和Critic在在线数据集中采样一些数据，先加入online Buffer中？
- checkpoints保存
#### Solution 1: 直接对输出层做Residual
#### Solution 2: 在表征层内做Residual
# 4. From-Scratch
- 能否在Online阶段直接生成一个from scratch的critic网络，并通过manual tuning，在online阶段将critic的控制权慢慢从offline阶段的critic变为online阶段的critic?
- 此时Actor使用Residual RL或者TSM Modelling?
# 5. BC-Distillation?
- BC-Penalty，能不能添加一个新的Actor，在Online阶段From-Scratch开始训练，有一个关于原Actor的BC Loss?
- ==或者说，我们根本就不用对Critic做修改==，在Actor上做文章即可，在Online阶段使用一个From Scratch的Actor，蒸馏获得原来的知识即可，或者在执行动作的时候加上对于offline actor给出动作的BC Loss，这个权重可以进行逐步的调整
- Critic，为了更好拟合online阶段的环境，是否可以通过一个Pre-Training的过程来进一步训练一个新的From Scratch的Critic，同时保留原来Critic的部分知识？而这个知识保留的过程可以通过一个权重系数来进行调整
# 6. BC-Mixed
- 能否借鉴FQL的思路，比如在online阶段开始之前，从头开始训练Actor和Critic，但是在Loss中添加Behaviour Cloning的项，这个Behaviour Cloning的系数是可以手动调整的，可以是常数，也可以是动态调整
- Actor还是Critic进行蒸馏?蒸馏是在online上进行还是offline数据集上进行？
- 从头开始训练的网络需要Partially-Frozen吗？还是正常网络？(直接利用短期训练的网络仍然有更高可塑性的特征)
- 是否能用offline阶段的Normalized Score来控制BC的强度？
# 7. Start with BC(Offline Policy)

# Dynamics Difference of BC&MaxQ
两种方法对梯度影响不同，也许可以参照PARS中对OOD的Penalization，防止OOD Overestimation
# Design Details
- ==Online Pre-Training Required?==
- Offline Algorithms/Online Algorithms?(IQL/SAC)
- How to Distill? KL-Divergence? Loss Definition? Threshold?
- Targetted at what networks? Offine/Online?
- Actor因为是概率分布，所以采用的是KL-蒸馏，Critic因为采用的是连续值输出，所以采用的是MSE-蒸馏
- 使用少量参数进行学习(是否可以加上L2 Norm限制范数增长过快)
- Hyperparameter中加上对于layernorm的设定，是true还是false