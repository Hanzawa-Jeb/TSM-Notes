全部改到JAX框架上
配置了A100二卡服务器
Antmaze
4.20正在跑JAX-IQL的500k, 800k, seed123
下一步可能要跑1000k和no crescln, seed123
挑选出最好的超参数，跑seed1-8
接下来调mujoco环境，IQL已经有mujoco的配置了，应该可以直接跑，就是要下一下d4rl的环境，要花点时间

后面跑Jax-CalQL
- 同样要跑其他hyperparameter
- 手动添加对mujoco的配置

Jax-IQL运行代码：
```bash
bash run.bash umaze,umaze_diverse,medium_play,medium_diverse,large_play,large_diverse 0,1 12 3 antmaze_gradual_ln_config 1 "--use_gradual_layernorm=True --lambda_decay_steps=200000"
```
环境，两张显卡，总线程数，seed重复次数，文件夹，base seed
Jax-CalQL
```bash
bash run.bash umaze,umaze_diverse,medium_play,medium_diverse,large_play,large_diverse 0,1 12 2 0 calql_no_ln_actor_distill "--gradual_layernorm=False --enable_distillation=True --actor_distill=True --critic_distill=False
```
环境，两张显卡，线程总数，seed重复次数，base seed

Jax-CalQL的这个case下面，目前来看，貌似no layernorm的case下的性能感觉还不错，我们可以多跑几个seed再看一下结果，先把antmaze跑完吧

Jax-IQL这个case下面，no layernorm的性能看起来也还不错

JAX-CalQL跑全量实验
```bash
bash run.bash umaze,umaze_diverse,medium_play,medium_diverse,large_play,large_diverse 0,1 24 5 6 calql_no_ln_actor_distill_full "--gradual_layernorm=False --enable_distillation=True --actor_distill=True --critic_distill=False"
```
JAX-IQL跑全量实验
`Usage: bash run.bash <env_list> [gpus] [threads] [times] [config] [base_seed] [save_dir] [extra_args]"`
IQL实际执行的指令
```bash
bash run.bash umaze,umaze_diverse,medium_play,medium_diverse,large_play,large_diverse 0,1 12 5 antmaze_gradual_ln_config 6 "/data/disk1/hyh/IQL-Results/noln-6-10" "--use_gradual_layernorm=False --lambda_decay_steps=200000"
```
2卡A100 40G刚才断了，不知道是什么原因

我们可以使用的Metrics:
- FAU，很明显
- weightnorm可以看，很明显
- 
- srank,好像不太明显，如果能做baseline对比的话也可以试试看
- gradient norm还没添加，要再添加一下看
- gradient covariance matrix可以再对比一下看看结果如何
注意：
`export XLA_PYTHON_CLIENT_PREALLOCATE=false`
取消显存预分配

mujoco locomotion
random, medium, medium-replay
hopper, halfcheetah, walker2d
3 times 3 = 9组实验


8卡A100上加一组calql的实验，带完整plasticity统计
```bash
bash run.bash umaze,umaze_diverse,medium_play,medium_diverse,large_play,large_diverse 2,4 10 5 6 calql_antmaze_seed6-10 "--gradual_layernorm=False --enable_distillation=True --actor_distill=True --critic_distill=False" "/data/disk1/hyh/calql-noln-antmaze-6-10"
```


当前总结：Antmaze 
IQL已经跑完了
- 但是gnorm作为plasticity参数还没有记录完全，后面可能要再补一下，先画一下别的图
CalQL正在跑
- 这里的Plasticity的记录是完整的
- 但是两个baseline都还没有跑
